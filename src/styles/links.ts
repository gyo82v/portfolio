import { focusEffects, transitions } from "./patterns"


export const resumeLinks = `inline-flex items-center gap-2 px-4 py-2 rounded-lg
                            ${focusEffects} ${transitions}
                            hover:scale-105 active:scale-95`